export default (state, action) => {
  switch(action.type){
    // Add Action for adding login user information
    // case: "ADD_GOOGLE_USER_INFO":
      // return {...state, loginUser: {userID: action.}}
    default:
      return state;
  }

}